import React from 'react';
import { storiesOf } from '@storybook/react';

import FormAndDropdown from './FormAndDropdown';

storiesOf('FormAndDropdown', module).add('default', () => <FormAndDropdown />);
